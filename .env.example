# GA4_MEASUREMENT_ID=G-XXXXXXXXXX
# Optional local copy: inject it at build time (see scripts/inject-config.mjs).
# Do NOT commit a real measurement id to a public repo if you want to keep it
# private — the Measurement ID is not a secret, but this keeps it out of history.
